/* Changing inner html with ID */
var output = document.getElementById("firstbird")
output.innerHTML = "<p>This is a paragraph with InnerHTML for class firstbird and colored by a DOM command. Check 'dom.js' </p>"
